name: tfg
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pytorch=2.2.0
  - torchvision
  - pytorch-cuda=12.1
  - pip
  - pip:
    - diffusers>=0.26.0
    - transformers>=4.40.0
    - accelerate>=0.30.0
    - wandb
    - safetensors
    - huggingface_hub>=0.23.0
    - scipy
    - pillow
    - tqdm
    - einops
